#!/bin/bash

# SuperWhisper - простой запуск из любой директории
# Использование: superwhisper (из любой директории)

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

echo "🎤 SuperWhisper - Локальная диктовка"
echo "===================================="

# Переход в директорию скрипта
cd "$SCRIPT_DIR"

# Проверка виртуального окружения
if [ ! -d "venv" ]; then
    echo "❌ Виртуальное окружение не найдено!"
    echo "Запустите сначала: ./install_and_run.sh"
    exit 1
fi

# Активация окружения
source venv/bin/activate

echo "🚀 Запуск SuperWhisper..."
echo ""
echo "⌨️  Управление:"
echo "   • Option + Space - запись/остановка"
echo "   • Иконка в строке меню: 🎤 (готов) / 🔴 (запись)"
echo "   • Текст автоматически вставляется в активное приложение"
echo ""
echo "🛑 Для остановки: Ctrl+C"
echo ""

# Запуск с обработкой прерываний
trap 'echo ""; echo "👋 SuperWhisper остановлен"; exit 0' INT TERM

./venv/bin/python superwhisper.py
