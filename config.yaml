# VTT (VoiceToText) - Конфигурация
# Локальная диктовка для macOS

app:
  version: "2.0"
  name: "VTT (VoiceToText)"

ui:
  auto_paste_enabled: true
  auto_paste_delay: 0.1
  auto_paste_force_mode: true  # 🆕 Принудительная вставка в любые приложения

audio:
  sample_rate: 16000
  max_recording_duration: 1800  # Увеличено до 30 минут
  vad_threshold: 0.5
  streaming:
    enabled: true
    chunk_duration: 5.0
    chunk_overlap: 0.5
    min_speech_duration: 0.25
    min_silence_duration: 0.1

whisper:
  model_name: "mlx-community/whisper-large-v3"
  language: "ru"
  task: "transcribe"
  device: "cpu"
  decoder_stability:
    temperature: 0.0
    # beam_size: 5  # MLX Whisper не поддерживает beam search, используем temperature-based sampling
    no_speech_threshold: 0.6
    condition_on_previous_text: true

punctuation:
  enabled: true
  mode: "improved"  # Используем улучшенный rule-based режим (без BERT)
  cache_dir: "./cache/punctuation"

  # Для включения BERT-модели (требует токен Hugging Face):
  # mode: "bert"
  # model:
  #   provider: "deeppavlov"
  #   name: "DeepPavlov/bert-base-cased-sentence"
  #   use_gpu: false

  model:
    provider: "none"  # Отключаем BERT-модель по умолчанию (не требует токена)
    name: "DeepPavlov/bert-base-cased-sentence"
    use_gpu: false
  rules:
    aggressive_commas: false  # Безопасный режим для запятых
    fix_abbreviations: true   # Исправлять аббревиатуры

vocabulary:
  enabled: true
  custom_terms_file: "./vocabulary/custom_terms.json"
  abbreviations_file: "./vocabulary/abbreviations.json"
  names_file: "./vocabulary/names.json"
  expand_abbreviations: true
  capitalize_names: true
  handle_compound_words: true

debloat:
  enabled: true
  aggressive_mode: false
  remove_fillers: true
  remove_echo_repeats: true
  quiet_mode: true  # Тихий режим для личного использования

numbers:
  enabled: true
  format_numbers: true
  format_percentages: true
  format_ranges: true
  format_units: true
  format_dates: true
  format_time: true
  format_file_sizes: true
  quiet_mode: true  # Тихий режим для личного использования

performance:
  force_garbage_collection: true  # Включаем для длинных записей
  clear_model_cache_after_use: true  # Очищаем кэш после использования
  memory_limit_mb: 2048  # Увеличиваем лимит памяти
  log_memory_usage: true  # Включаем логирование памяти
  chunk_processing:
    enabled: true  # Включена чанковая обработка
    chunk_threshold_sec: 300  # Увеличиваем порог до 5 минут для комфортной работы
    max_chunk_duration_sec: 60  # Увеличиваем размер чанка
    chunk_overlap_sec: 3  # Небольшое перекрытие для плавности

debug:
  enable_detailed_logs: true
  log_level: "INFO"