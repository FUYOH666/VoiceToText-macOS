---
alwaysApply: false
description: Development workflow and testing guidelines for SuperWhisper
---

# Development Workflow для SuperWhisper

## 🎯 Философия разработки

### Минимализм
- Только необходимые компоненты
- Чистый код без излишеств
- Фокус на core functionality

### Надежность
- Graceful error handling
- Подробное логирование
- Fallback механизмы

### Производительность
- Оптимизация для Apple Silicon
- Эффективное управление памятью
- Быстрый запуск

## 🚀 Локальная разработка

### 1. Настройка окружения
```bash
# Клонирование
git clone https://github.com/FUYOH666/VoiceToText-macOS.git
cd VoiceToText-macOS

# Установка
./install_and_run.sh
```

### 2. Запуск в режиме разработки
```bash
# С подробными логами
./venv/bin/python superwhisper.py

# С сохранением логов
./venv/bin/python superwhisper.py > debug.log 2>&1
```

### 3. Тестирование компонентов
```bash
# Базовые тесты
./venv/bin/python test_basic.py

# Тест автовставки
./venv/bin/python test_auto_paste_debug.py
```

## 🔧 Работа с кодом

### Структура проекта
```
SuperWhisper/
├── superwhisper.py          # 🎯 Главный файл
├── src/                     # 📁 Исходный код
│   ├── whisper_service.py   # 🧠 Распознавание речи
│   ├── auto_paste.py        # 📋 Автовставка
│   ├── punctuation_service.py # ✏️ Пунктуация
│   └── ...                  # Другие сервисы
├── config.yaml             # ⚙️ Конфигурация
└── README.md               # 📖 Документация
```

### Добавление новой функциональности

1. **Создать сервис в `src/`**
   ```python
   # src/my_service.py
   import logging

   class MyService:
       def __init__(self, config):
           self.config = config
           self.logger = logging.getLogger(__name__)
   ```

2. **Интегрировать в главный файл**
   ```python
   # superwhisper.py
   from src.my_service import MyService

   def _init_services(self):
       self.my_service = MyService(self.config)
   ```

3. **Добавить логирование**
   ```python
   self.logger.info("Мой сервис инициализирован")
   self.logger.debug(f"Настройки: {self.config}")
   ```

## 🧪 Тестирование

### Уровни тестирования

#### 1. Модульные тесты
```python
# test_my_service.py
from src.my_service import MyService

def test_my_service():
    config = {"setting": "value"}
    service = MyService(config)
    assert service.config == config
```

#### 2. Интеграционные тесты
```python
# Тест взаимодействия сервисов
def test_services_integration():
    config = Config("config.yaml")
    whisper = WhisperService(config)
    punctuation = PunctuationService(config)

    # Тест пайплайна
    audio = load_test_audio()
    text = whisper.transcribe(audio)
    result = punctuation.restore_punctuation(text)
    assert len(result) > 0
```

#### 3. Ручное тестирование
```bash
# Тест в реальных условиях
./venv/bin/python superwhisper.py

# Тест с отладкой
./venv/bin/python test_auto_paste_debug.py
```

### Тестовые данные
- Использовать короткие аудио файлы
- Тестировать edge cases (пустой текст, длинный текст)
- Проверять различные приложения для вставки

## 📊 Мониторинг и отладка

### Логирование
```python
# Все сервисы используют структурированное логирование
self.logger.info("Операция начата")
self.logger.debug(f"Детали: {details}")
self.logger.error(f"Ошибка: {e}")
```

### Профилирование
```python
import time

start_time = time.time()
result = expensive_operation()
duration = time.time() - start_time
self.logger.info(f"Операция заняла: {duration:.2f} сек")
```

### Memory monitoring
```python
import psutil

def log_memory_usage():
    process = psutil.Process()
    memory_mb = process.memory_info().rss / 1024 / 1024
    self.logger.debug(f"Память: {memory_mb:.1f} MB")
```

## 🔄 Рабочий процесс Git

### Feature development
```bash
# Создать ветку
git checkout -b feature/my-feature

# Разработка
# ... кодим ...

# Тестирование
./venv/bin/python test_basic.py

# Коммит
git add .
git commit -m "feat: добавить новую функцию

- Добавлен новый сервис
- Улучшена обработка ошибок
- Добавлены тесты"

# Push
git push origin feature/my-feature
```

### Code review
- Проверить логику
- Убедиться в наличии тестов
- Проверить производительность
- Валидировать логирование

## 🚀 Релиз

### Подготовка релиза
```bash
# Финальное тестирование
./install_and_run.sh
./venv/bin/python test_auto_paste_debug.py

# Очистка
rm -rf cache/ __pycache__/

# Создание тега
git tag -a v1.1.0 -m "Release v1.1.0"
git push origin v1.1.0
```

### Post-release
- Обновить документацию
- Проверить обратную связь
- Мониторить issues

## 📈 Производительность

### Метрики для мониторинга
- Время запуска приложения
- Скорость распознавания речи
- Точность пунктуации
- Успешность автовставки
- Использование памяти

### Оптимизации
- Использовать MLX вместо PyTorch для Apple Silicon
- Ленивая загрузка моделей
- Принудительная сборка мусора
- Асинхронная обработка тяжелых операций

## 🐛 Обработка ошибок

### Graceful degradation
```python
try:
    result = risky_operation()
except Exception as e:
    self.logger.error(f"Ошибка: {e}")
    result = fallback_operation()
```

### User-friendly messages
```python
# Вместо технических деталей
# "ImportError: No module named 'mlx_whisper'"
# Показывать:
# "Не установлены зависимости. Запустите ./install_and_run.sh"
```

## 📚 Документация

### README.md
- Краткое описание
- Быстрый старт
- Требования к системе
- Решение проблем

### Inline документация
```python
def complex_function(self, param1: str, param2: int) -> dict:
    """
    Описание что делает функция.

    Args:
        param1: Описание параметра
        param2: Описание параметра

    Returns:
        Описание возвращаемого значения

    Raises:
        SpecificException: Когда происходит ошибка
    """
```

## 🎯 Лучшие практики

### Code quality
- Использовать type hints
- Документировать сложные функции
- Следовать принципам SOLID

### Git hygiene
- Маленькие, focused коммиты
- Описательные сообщения
- Регулярные пушы

### Testing
- Тестировать edge cases
- Проверять error conditions
- Мониторить performance regression