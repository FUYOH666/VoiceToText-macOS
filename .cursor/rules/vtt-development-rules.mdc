---
alwaysApply: true
description: "Правила разработки для проекта VTT (VoiceToText) - локальная диктовка для macOS"
globs: *.py,*.yaml,*.yml,*.md,*.sh,*.txt
---

# 🎤 VTT (VoiceToText) - Правила разработки

## 🎯 Цели проекта

VTT - это локальное приложение для macOS, которое позволяет диктовать текст в микрофон и получать чистый, обработанный текст **без облаков и подписок**.

### Основные возможности

- **Оффлайн обработка** через MLX Whisper
- **Умная постобработка**: debloat, форматирование чисел, словарь терминов
- **Исправление аббревиатур**: MLX Whisper, Voice-to-Text, macOS
- **Обработка дат и времени**: годы, время, размеры файлов
- **Профессиональная пунктуация**: BERT-модель
- **Автовставка текста** с горячими клавишами (Option + Space)
- **Тихий режим** для личного использования

### 📊 Статус проекта

- ✅ **Полный аудит репозитория** завершен (2025-01-09)
- ✅ **483 строки** неиспользуемого кода удалены
- ✅ **Структура оптимизирована** - только актуальные файлы
- ✅ **GitHub шаблоны** обновлены под VTT
- ✅ **Продакшен-ready** - чистый, протестированный код
- ✅ **Критические ошибки исправлены** - нет больше Abort trap и Trace/BPT trap
- ✅ **Стабильная работа** - полная синхронизация обработки речи
- ✅ **Качество++** - улучшения транскрипции (v2.0.5)
- ✅ **Умная постобработка** - BERT пунктуация, исправление аббревиатур

## 🏗️ Архитектура проекта

### Структура файлов

- `src/` - 12 оптимизированных сервисов (после аудита)
- `vocabulary/` - словари для исправлений
- `models/` - ⚠️ **КРИТИЧНО**: модель Whisper (не удалять!)
- `.github/` - шаблоны для issues и PR
- [config.yaml](mdc:config.yaml) - единая конфигурация
- [requirements.txt](mdc:requirements.txt) - зависимости
- [install_and_run.sh](mdc:install_and_run.sh) - скрипт установки (исправлен баг с виртуальным окружением)

### Ключевые сервисы (все используются)

- **WhisperService**: MLX Whisper интеграция
- **SpeechProcessor**: синхронная обработка речи (ранее AsyncSpeechProcessor)
- **VocabularyService**: кастомный словарь терминов
- **DebloatService**: очистка от слов-паразитов
- **NumberService**: форматирование чисел
- **AutoPasteService**: автовставка текста
- **HotkeyManager**: управление горячими клавишами
- **AudioRecorder**: запись аудио
- **MemoryManager**: управление памятью
- **NotificationService**: системные уведомления
- **PunctuationService**: восстановление пунктуации

## 📋 Правила разработки

### 1) Python & окружение

- ✅ **Целевая версия**: Python 3.12
- ✅ **Виртуальное окружение**: всегда `./venv/` в корне проекта
- ✅ **Установки**: `./venv/bin/python -m pip install ...`
- ✅ **Pip**: обновлять до `pip==25.2`
- ❌ **Запрещено**: system Python, глобальные установки

### 2) Зависимости

- ✅ **Все версии пиновать** в [requirements.txt](mdc:requirements.txt)
- ✅ **Изменения зависимостей** = PR/ревью
- ✅ **Хранить** `requirements.txt` в репозитории

### 3) Конфигурация

- ✅ **Один файл**: [config.yaml](mdc:config.yaml)
- ✅ **Переопределение**: ENV-переменные имеют приоритет
- ✅ **Валидация**: при старте через pydantic/voluptuous
- ✅ **Fail fast**: при ошибках конфигурации

### 4) Логирование

- ✅ **Использовать `logging`**, не `print`
- ✅ **Формат**: `timestamp level service message meta`
- ✅ **Контекст**: имя файла/задачи/параметры в ошибках
- ✅ **Health check**: команда для диагностики

### 5) Качество кода

- ✅ **Docstring** для каждой функции
- ✅ **Линтинг**: `ruff`, форматирование: `black`
- ✅ **Типы**: `mypy` по возможности

### 6) Документация

- ✅ [README.md](mdc:README.md) с установкой/запуском
- ✅ [PERSONAL_USE_GUIDE.md](mdc:PERSONAL_USE_GUIDE.md) для пользователей
- ✅ [CHANGELOG.md](mdc:CHANGELOG.md) с историей изменений

## 🚀 GitHub стиль

### Вайб: Саркастический маркетинг-лайт

- **Лозунги**: Короткие, без кринжа
- **Тон**: Прямой, уверенный, с иронией к сложным решениям

### Примеры

```markdown
🎤 VTT - Шепчешь в микрофон, получаешь текст
> Без облаков. Без подписок. Без "умных" помощников. Просто работает.
```

### Коммиты

- "🚀 Качество++ - исправил аббревиатуры, числа и де-болтовню, потому что 'эээ' никому не нужны"
- "Добавил де-болтовню, потому что 'эээ' никому не нужны"
- "Фикс чисел - теперь 3500 выглядит как 3 500, а не как заклинание"
- "Исправил Whisper артефакты - больше никаких 'Apple MLX framework' в тексте"

### PR описания

- "Новая фича: теперь понимает когда ты 'короче говоря' имеешь в виду 'проще сказать'"

## 🎨 Стиль кода

### Функции

```python
def process_text(self, text: str) -> str:
    """Обрабатывает текст с использованием словаря"""
    # Код здесь
```

### Логирование

```python
self.logger.info("📚 VocabularyService инициализирован")
self.logger.error(f"Ошибка обработки: {e}")
```

## 🔧 Специфика VTT

### MLX Whisper

- **Оптимизация**: для Apple Silicon (M1/M2/M3)
- **Guard-проверка**: устройства при старте
- **Очистка памяти**: после каждой транскрипции

### Постобработка

- **Debloat**: фильтр слов-паразитов (50+ конструкций)
- **Numbers**: тонкие пробелы, проценты, диапазоны, даты, размеры файлов
- **Vocabulary**: исправление терминов и аббревиатур + Whisper артефакты
- **Punctuation**: BERT-модель для интеллектуальной пунктуации
- **Text Formatting**: автоматическое исправление тире, пробелов, кавычек

### ⚠️ Критичные компоненты (НЕ УДАЛЯТЬ)

#### Модель Whisper
- **Расположение**: `./models/`
- **Файлы**: `config.json`, `weights.npz`
- **Размер**: ~3GB
- **Источник**: `mlx-community/whisper-large-v3-mlx`
- **Статус**: Не коммитится в git (слишком большой)
- **Установка**: Скачивается отдельно при первом запуске

**ПРАВИЛО**: Никогда не удалять папку `models/` - без модели приложение не работает!

### Конфигурация

```yaml
# Тихий режим для личного использования
debloat:
  enabled: true
  quiet_mode: true

numbers:
  enabled: true
  quiet_mode: true
```

## ✅ Definition of Done

- [ ] Код в `./src`, зависимости пинованы
- [ ] [config.yaml](mdc:config.yaml) + валидация
- [ ] Логи информативны, есть health check
- [ ] README покрывает установку/запуск
- [ ] Нет заглушек, работает оффлайн
- [ ] MLX Whisper с guard-проверкой устройства
